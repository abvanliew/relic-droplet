import React from 'react';
import Grid from './panels/Grid';

export default class App extends React.Component {
  render() {
    return (
      <React.Fragment>
        <Grid/>
      </React.Fragment>
    );
  }
}